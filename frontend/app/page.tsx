// llm-cockpit landing page — STUB
// Real auth flow + dashboard land per SPEC-001/002.

export default function Home() {
  return (
    <main className="flex min-h-screen items-center justify-center p-8">
      <div className="max-w-lg space-y-4 text-center">
        <h1 className="text-3xl font-semibold">llm-cockpit</h1>
        <p className="text-sm text-neutral-500">
          Scaffold. Login + dashboard land per SPEC-001 and SPEC-002.
        </p>
        <p className="text-xs text-neutral-400">
          Source of truth: <code>020 Projects/LLM-Cockpit/</code> in the obsidian vault.
        </p>
      </div>
    </main>
  );
}
